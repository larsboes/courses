apiVersion: v1
kind: Service
metadata:
  name: html-webserver
spec:
  type: NodePort
  selector:
    component: webserver
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30006
