apiVersion: v1
kind: Service
metadata:
  name: flask-webserver
spec:
  type: NodePort
  selector:
    component: webserver
  ports:
    - protocol: TCP
      port: 8001
      targetPort: 8001
      nodePort: 30001
