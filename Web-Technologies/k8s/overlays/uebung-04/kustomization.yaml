apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: fhdw-wta

resources:
  - ../../base
  - flask-deployment.yaml
  - flask-service.yaml

commonLabels:
  exercise: uebung-04
  app: simple-webserver
