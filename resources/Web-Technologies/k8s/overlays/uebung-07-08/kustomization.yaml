apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: fhdw-wta

resources:
  - ../../base
  - nginx-deployment.yaml
  - nginx-service.yaml
  - nginx-configmap.yaml

commonLabels:
  exercise: uebung-07-08
  app: meta-playlist-ui
