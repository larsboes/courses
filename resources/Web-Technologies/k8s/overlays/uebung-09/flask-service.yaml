apiVersion: v1
kind: Service
metadata:
  name: rest-api-server
spec:
  type: NodePort
  selector:
    component: api
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30009
